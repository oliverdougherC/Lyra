# Product screenshot provenance

`lyra-workspace.png` was captured September 6, 2026 from the integrated production frontend in
Chromium at 1440 × 1000. It uses the synthetic class/document/study/writer fixtures in
`frontend/e2e/viewport-matrix.spec.ts` and mocked API/native bootstrap responses. It is a genuine
rendering of the current components, not generated artwork or an edited DOM.

The screenshot contains no operator course data. It illustrates the frontend class workspace;
it is not evidence of a native macOS session, a live provider result, or release approval.
Replace it when the visible interface changes and record the new capture context here.

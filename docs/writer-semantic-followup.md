# Writer semantic follow-up: PLA-152 / PLA-153

[PR #86](https://github.com/oliverdougherC/Lyra/pull/86) is stacked on the unchanged, reviewed [PR #83](https://github.com/oliverdougherC/Lyra/pull/83), head `0b7aca82f1922c664af15a07888d44d90be0c0a1`. Tested application source is **`e2857f61cbe04b9bde533419b0f393bcaa1dfa11`**, fingerprint `b9cc1e055ab6bf88274bdc1208f59fb182c536344a1721a44a4460052bdee185`. This focused addendum supplies evaluator evidence; **PLA-152 remains In Review for owner closure, and PLA-153 quality remains open**. It does not supersede the [reviewed recovery packet](writer-beta-evidence.md), merge either PR, or claim human acceptance.

The [causal trace](writer-semantic-followup/negative-trace.json) links existing reviewed receipts rather than copying their history. A fresh, fingerprint-valid [baseline run](writer-semantic-followup/before/report.json) and [wire frames 16–18](writer-semantic-followup/wire/delivery-summary.json) distinguish source availability from delivery: assessment frame 16 received the complete 18-response and 14-response facts; revision frames 17–18 did not receive the complete 14-response fact. A digit in a correction instruction is not delivered source evidence. The follow-up supplies the same saved evidence to correction calls and requests exact `before`/`after` edits through `writer_scoped_revision`. A [representative final scoped call](writer-semantic-followup/wire/delivery-summary.json) retains the source facts, revision context and scope contract. Companion assessment frame 66 records `max_tokens=4096` and `finish_reason=length`: an assessment-output ceiling, not missing revision context, a scoped-edit-format budget or grounds to raise limits. This is a measured handoff repair, not proof that every model judgment is correct.

There are **11 final source runs plus two frozen reruns of the same two unique heldouts**: five targeted cases, three full-live controls, one generated-plan run, and two separately authored heldouts. The [independent agent review](writer-semantic-followup/independent-review.json) records 11 completed observations and two failed controls. Seven unique targeted/heldout cases repair their central issues; both frozen heldouts preserve valid facts and exact student requirements. Historical attribution remains ambiguous in one replacement-source proposal; the deleted-source proposal repeats a repair. Full-live outputs still contain unsupported population/method claims or an underdeveloped next step. The two failures occurred **after inference** (paragraph length and review output ceiling): useful partials survived, but neither is successful delivery or a preflight refusal. Standalone reviewer and long-budget runs were not repeated in this follow-up. Scores remain separate judgments, never an acceptance average; the [human decision table](writer-semantic-followup/human-review.md) is entirely unchecked.

| PLA-152 criterion | Delivered evidence / limit |
|---|---|
| 1. Versioned briefs, sources, states, plans, comments and expected notes | Existing [corpus](../scripts/eval_corpora/writer_quality.v1.json), [heldouts](../scripts/eval_corpora/writer_semantic_holdout.v1.json) and [rubric](writer-quality-rubric.md); no duplicate corpus here. |
| 2. Meaningful student prose; no silent replacement/homogenization | Raw before/after bodies and protected-passage checks; voice separately judged. Preservation does not certify voice quality. |
| 3. Supported and unsupported claims | Seed adjudication, saved revision evidence and captured delivery; citation markers alone are not support. |
| 4. Separate planning, evidence/citation, revision, comments and instruction measures | Rubric and per-dimension results; unexercised dimensions explicitly N/A. |
| 5. Human/judge dimensions distinguished from deterministic truth | Independent-agent identity/version limits explicit; human verdicts unchecked. Human acceptance is not an added evaluator-methodology prerequisite. |
| 6. Empty, unrelated, duplicate and unsupported seeds | Corpus plus [two actual-HTTP checks](writer-semantic-followup/validation.json): empty replacement and an off-target quoted paragraph. Empty research is not empty-rewrite coverage; off-target scoped coverage is not old-protocol equivalence. |
| 7. Prompt/model/runtime identity with results | Per-group reports and [hash manifest](writer-semantic-followup/manifest.json). Model label is `Qwen3.8-27B`; exact weights are unknown. |
| 8. Comparison format exposing dimension regressions | Existing comparison script and compact agent/human tables. Generated plans, seeded plans and same-model interruptions remain distinct. |
| 9. Baseline before tuning | Original current-main `e96bf488` blocked/partial baseline remains in the reviewed packet; early missing configuration/corpus/source fields stay disclosed. Fresh `0b7` wire reproduction is one case, not a complete matched nine-case baseline. |

The [validation record](writer-semantic-followup/validation.json) records **3,379 passed, one skipped** on final source, plus two separate current HTTP checks. Frozen heldout runtime hash is `a5420ba82e53d27ef8969836ac223fd5c8376a42ab9a506ed8a139b6eac42a4c`. Reports say `loopback` because a capture service forwarded to the explicitly authorized **non-loopback** provider; that is first-hop locality, not proof the model ran locally. Raw-input and sanitized-retained hashes are separate. Endpoints, credentials, user/temp paths, helper identities, nonces and profiles are not included.

[Native isolation proof](writer-semantic-followup/native-proof.json) covers 12 expected guard outcomes, a compiled unique test identity, a nonpersistent WebView store, and two guarded LaunchServices launches with native/backend selectors and exits verified. Original candidate/backend fingerprints remained unchanged. CUA timed out; bundle-ID lookup failed closed; only exact guarded-path reopening succeeded. This identity/store variant is **not production-native or native Keychain acceptance**. Earlier CUA selection auto-launched the normal profile. It supplied no corpus data, but startup effects—including possible default WebKit storage effects—remain uncertain. Prior backend-only selector checks do not establish whole-native isolation or no impact. PLA-153 retains semantic quality, production-native/Keychain, longer-session and release-owner acceptance work; no status is marked Done here.

Exact executed argument templates are retained in the validation record. Use an `e2857f6` source checkout for exact Git identity; later evidence/test commits do not change the frozen application. Reproduction uses the unchanged runner, explicit isolated provider configuration, and a signed matching bundle. These commands send only synthetic fixtures; capture-proxy deployment is separate from the runner and is not included in this packet:

```sh
uv run --extra dev pytest -q
uv run pytest backend/tests/test_writer_empty_prose_http.py -q
uv run python scripts/eval_writer.py --endpoint "$LYRA_EVAL_ENDPOINT" --model Qwen3.8-27B --context-window 262144 --allow-remote --output /tmp/writer-targeted --case pass_targeted_causal_precision --case pass_reject_empty_and_unrelated_rewrite --case source_history_replace --case source_history_delete --case pass_multisection_same_model_recovery --timeout 630
uv run python scripts/eval_writer.py --endpoint "$LYRA_EVAL_ENDPOINT" --model Qwen3.8-27B --context-window 262144 --allow-remote --output /tmp/writer-controls --case full_live_draft_from_student_notes --scenario uninterrupted --scenario restart_review --scenario edit_cancel_retry --timeout 630
uv run python scripts/eval_writer.py --endpoint "$LYRA_EVAL_ENDPOINT" --model Qwen3.8-27B --context-window 262144 --allow-remote --output /tmp/writer-planning --case full_live_draft_from_student_notes --generate-plan --timeout 630
uv run python scripts/eval_writer.py --endpoint "$LYRA_EVAL_ENDPOINT" --model Qwen3.8-27B --context-window 262144 --allow-remote --corpus scripts/eval_corpora/writer_semantic_holdout.v1.json --output /tmp/writer-heldout --timeout 630
uv run python scripts/eval_writer.py --endpoint "$LYRA_EVAL_ENDPOINT" --model Qwen3.8-27B --context-window 262144 --allow-remote --corpus scripts/eval_corpora/writer_semantic_holdout.v1.json --output /tmp/writer-frozen-heldout --timeout 630 --backend-executable src-tauri/target/release/bundle/macos/Lyra.app/Contents/Resources/resources/lyra-backend/lyra-backend
```

For source/frozen comparison, use `uv run python scripts/compare_writer_evals.py --baseline /tmp/writer-heldout --candidate /tmp/writer-frozen-heldout --output /tmp/writer-heldout-comparison.json`; inspect per-case dimensions and failures rather than treating successful recording as acceptance.

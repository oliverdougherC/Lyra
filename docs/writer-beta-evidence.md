# Writer/reviewer beta evidence: PLA-152 and PLA-153

This packet retains the baseline, intermediate repairs, negative model findings, and tested packages for PLA-152/153. Production source is frozen at **24575e9f859a220440186c106a8936b303a6510e**. All final runs and their receipts are complete; earlier 1d/39/d181 results remain intermediate and retain their own source identities. Earlier candidate designations were withdrawn when independent review found incorrect source IDs, duplicated sibling sections, and a generated-plan handoff that contradicted the requested structure. **No blanket quality or release acceptance is claimed.**

A later documentation-only commit is an evidence head, not a new tested application SHA. Match source, runtime, corpus, and individual receipt fingerprints; do not treat folder names or evaluator labels such as “final” as release status. Earlier 86a34f7 evidence below remains intermediate historical evidence.

The baseline is **e96bf4886977c648f9e7905c7807c806b1ae7a80**. PLA-464–468 protections remain requirements: evidence provenance, terminal states, mandatory-input budgets, complete streams, and deadlines must not be relaxed to get a run to finish.

## Frozen source 24575e9: current review handoff

**Application source:** `24575e9f859a220440186c106a8936b303a6510e`. **Review:** [PR #83](https://github.com/oliverdougherC/Lyra/pull/83). A later evidence-only commit must remain distinct from this tested application source.

The final package matrix contains **33 receipts: 31 completed current runs and two honest failures; 24 observed interventions and nine controls, with no requested boundary unobserved in this selected final matrix**. The failures are mandatory-context refusal and the real edited-cancel/retry output cutoff. Source controls are separate. The [independent recovery audit](writer-beta-evidence/24575e9-recovery-adjudication.json) records the captured invariants and limitations. In particular, the controlled empty fault targets research output, not an empty-prose rewrite; a repeated paragraph/missing headings already exist in the targeted synthetic control and are not restart-added defects. These fixture outputs do not establish semantic acceptance.

The [packet validation receipt](writer-beta-evidence/packet-validation.json) verifies 348 retained hashes, 45 corpus hashes, all 33 final package identities, the 12 independently graded receipt hashes, and the final CI source identity. Privacy, local-link, active-reference, and diff checks passed.

The [final-source validation receipt](writer-beta-evidence/validation-24575e9.json) records **3,362 tests passed, 1 skipped** with `uv run --extra dev pytest -q`. The [signed package receipt](writer-beta-evidence/24575e9-package.json) records app executable, backend executable, plist, and complete bundle-tree hashes. Authenticated frozen smoke and the 77-object bundle verifier passed; the build used the reused Apple Development identity. The [24575e9 isolated native receipt](writer-beta-evidence/native-24575e9.json) verifies synthetic native/backend profile selectors, the empty Classes screen, and both processes exiting on Cmd+Q. The host had 24 GiB RAM; this does **not** satisfy the unrun 8 GiB gate.

| Final-source receipt | Recorded outcome | Scope |
| --- | --- | --- |
| [Real generated planning](writer-beta-evidence/24575e9-planning/report.json) | Completed; two required sections and original notes retained. | Separate generated-plan experiment. |
| [Real seeded-plan control](writer-beta-evidence/24575e9-model/report.json) | Completed; original notes retained. | Supplied plan, so no generated-planning score. |
| [Controlled source corpus](writer-beta-evidence/24575e9-controlled-corpus/report.json) | Eight completed, one expected mandatory-context refusal. | Nine development-corpus cases; scripted provider. |
| [Packaged controlled writer](writer-beta-evidence/24575e9-packaged-controlled/report.json) | Twelve named cases, with all requested interventions observed. | Failed/cancelled predecessors remain recorded when explicit retries complete. |
| [Packaged controlled reviewer](writer-beta-evidence/24575e9-packaged-review/report.json) | Six named cases, including persistence boundary and provider-failure retries. | Real frozen HTTP workflows, scripted inference. |
| [Packaged section recovery](writer-beta-evidence/24575e9-packaged-section/report.json) | Control and between-section restart recorded. | Independent section-workflow coverage. |
| [Packaged source/context cases](writer-beta-evidence/24575e9-packaged-sources/report.json) | Replacement/delete cases completed; tight-context case refused honestly. | Historical supporting-revision invariants and mandatory-budget behavior. |
| [Packaged real full-live series](writer-beta-evidence/24575e9-packaged-model/report.json) | Six completed; edited cancel/retry successor failed on output-token ceiling and retained edited text/useful partials. | Same Qwen3.8-27B model and frozen package; includes student edit/restart and edit/cancel/retry. |

The [source/package comparison](writer-beta-evidence/24575e9-source-package-comparison.json) and [same-package interruption comparison](writer-beta-evidence/24575e9-package-interruption-comparison.json) each retain seven pairs and return `acceptance: not_determined`. The latter uses a [control-only report slice](writer-beta-evidence/24575e9-packaged-model-control/report.json), preserving original case and corpus bytes. They do not compare different model families.

The [final-source separate-agent review](writer-beta-evidence/independent-24575e9-review.json) covers **12 receipts**: two source controls, seven real packaged full-live scenarios, packaged generated planning, and two real reviewer cases. In the two source controls, the previous false deletion of supported survey/method facts was absent. Generated planning scored 3 in planning/evidence/revision/review/instruction dimensions but **1 in student voice**; the seeded control scored evidence/citation 4 and revision/review/instruction/voice 3. Remaining voice flattening and some overconfident measurement language are semantic owner-review concerns. They are not erased by byte-preserved accepted text or completed run status. No scores are averaged into acceptance. The packaged samples still show recurring semantic review errors: deletion of supported statistics, an invented week label, and an invented low-effort claim in the failed partial. These remain explicit owner/human review concerns.

[Packaged generated planning](writer-beta-evidence/24575e9-packaged-planning/report.json) completed. The [two final real reviewer receipts](writer-beta-evidence/24575e9-packaged-model-review/report.json) completed: control in 505.026 seconds and observed persistence restart in 433.993 seconds, all four lenses finished. Their [same-package comparison](writer-beta-evidence/24575e9-package-review-comparison.json) is retained. Initial comment 1 and pre-cut comment 2 survived exactly. Final accounting is seven comments added after restart plus two earlier comments; eight were created across the entire run. The required causal seed was covered (missed 0), but fresh independent rediscovery is not asserted because an existing finding already covered it. Exact and equivalent actionable duplicates were 0; one redundant “No new findings” note remains. Planning was not scored for these supplied-plan reviewer cases, and revision precision was not applicable. A decrease in severity counts is observational, not proof of improvement. [Final CI](https://github.com/oliverdougherC/Lyra/actions/runs/34082749319) has all 11 checks successful; the [run identity receipt](writer-beta-evidence/ci-run-24575e9.json) independently verifies `headSha` equals the frozen 24575e9 source, and the [check snapshot](writer-beta-evidence/24575e9-ci.json) retains individual job links. Prior targeted negative receipts remain relevant and source-specific; the final full-live controls are not a replacement for unperformed final targeted or held-out quality evaluation.

## Intermediate 1d046a2: planning repaired, internal review still incomplete

The [1d validation receipt](writer-beta-evidence/validation-1d046a2.json) records **3,361 tests passed, 1 skipped**, a signed app, bundle verification, and authenticated frozen smoke. No 1d packaged evaluation series was started before the next defect was identified.

The [real generated-plan run](writer-beta-evidence/1d046a2-planning/report.json) now formed the requested **Evidence** and **Recommendation** sections, but failed final paragraph-length validation and kept partial writing. The [seeded-plan control](writer-beta-evidence/1d046a2-model/report.json) completed. The [separate-agent adjudication](writer-beta-evidence/independent-1d046a2-review.json) found that internal review in both cases wrongly removed the supported fact that 18 riders responded: selected excerpts lacked the detail, while the saved source contained it. The generated output also lost student voice. This is a concrete remaining context-delivery defect, not proof that the source fact was absent. The next bounded repair gives internal review saved-source context for the source IDs it already collected, keeping current and historical evidence distinct. These negative receipts remain part of the handoff.

## Intermediate 39e7352: guards, package recovery, and planning defect

Source **39e7352193fd25efd9ac1b5864765d618308c5ff** is a formatting-only follow-up to a37daaee497b7e3b4e26fa040edbfd3c2dd3108b. Its [validation receipt](writer-beta-evidence/validation-39e7352.json) records **3,357 tests passed, 1 skipped**, stable-identity signing, bundle verification, and authenticated frozen smoke. These receipts do not cover the later 1d planning repair.

The [39 real source controls](writer-beta-evidence/39e7352-model/report.json) contain four honestly failed targeted/multisection runs and one completed full-live run. Citation/section-boundary guards prevented invalid prose from being silently accepted; remaining automatic-review findings still kept partial runs failed. [a37 targeted negative outputs](writer-beta-evidence/a37daae-model/report.json) remain because the [separate-agent review](writer-beta-evidence/independent-39e7352-review.json) explicitly cites them. Do not transfer their grades to different 39 samples. The a37 full-live receipt has source drift and is excluded from controlled attribution.

The directory named [a37daae-planning](writer-beta-evidence/a37daae-planning/report.json) actually records **39e7352**, `generate_plan=true`, and stable source. It completed, but independent adjudication gave planning/evidence/instruction scores of 1: wrong two-section structure, unsupported unique-person inference, misattributed source roles, an invented service goal, and an unsupported direction of sample bias. A concrete handoff defect was then found: the live planner was told to reuse notes headings, and request/brief analysis was omitted from final section mapping. The 1d source repairs that handoff; completion alone did not validate this generated plan.

The [39 immutable packaged-model series](writer-beta-evidence/39e7352-packaged-model/report.json) has a completed control plus six observed full-live intervention runs, including real student block edits and cancellation/retry. All seven captured source-drift flags are false. Their [same-package comparison](writer-beta-evidence/39e7352-package-interruption-comparison.json) uses an explicitly derived [uninterrupted control slice](writer-beta-evidence/39e7352-packaged-model-control/report.json); it is not a comparison of each case against itself. The [source/package comparison](writer-beta-evidence/39e7352-source-package-comparison.json) keeps runtime identity separate. Both return acceptance undetermined. The separately queued 39 real reviewer series was cancelled before starting and is **not run**.

Controlled frozen coverage at 39 includes [12 writer scenarios](writer-beta-evidence/39e7352-packaged-controlled/report.json), [six reviewer scenarios](writer-beta-evidence/39e7352-packaged-review/report.json), [two targeted section scenarios](writer-beta-evidence/39e7352-packaged-section/report.json), and [three source-history/context cases](writer-beta-evidence/39e7352-packaged-sources/report.json). Requested interventions in those records were observed; the long-context case refused rather than discarding mandatory content. Scripted inference verifies recovery invariants, not real-model semantic quality.

## Earlier d18118b validation and negative findings

The [d181 validation receipt](writer-beta-evidence/validation-d18118b.json) records **3,345 tests passed, 1 skipped**, using `uv run --extra dev pytest -q`. [CI run 34080748660](https://github.com/oliverdougherC/Lyra/actions/runs/34080748660) has **11 successful checks**, including CI Gate and Desktop macOS artifact; the [retained check receipt](writer-beta-evidence/d18118b-ci.json) preserves their links. The earlier `hdiutil` artifact failure is historical and resolved for this run. Green infrastructure checks do not override the negative output review.

The d181 app had 78 code objects signed with the reused Apple Development identity; 77 objects passed the bundle verifier. Authenticated frozen-backend smoke passed. The [isolated native receipt](writer-beta-evidence/native-d18118b.json) verifies separate synthetic native/backend data, database, cache, logs, models, and Keyring selectors; the UI showed **“No classes yet”**, and Cmd+Q exited both processes. This is launch/isolation/exit smoke, not writing-quality or human acceptance.

An earlier **86a34f7 CUA app selection automatically launched the normal profile**. It was not used for corpus cases or intentionally modified, but startup side effects cannot be excluded. That launch is excluded from isolated acceptance evidence. Later isolated 00371f3 observation and the retained d181 isolated smoke are separate runs; neither retroactively validates the 86 launch.

| d181 receipt | Observed result | Interpretation |
| --- | --- | --- |
| [Source controlled corpus](writer-beta-evidence/d18118b-controlled-corpus/report.json) | Nine scripted-provider cases: eight completed, long mandatory-context case refused without discarding writing. | Deterministic behavior only; no semantic quality score. |
| [Real model corpus](writer-beta-evidence/d18118b-model-corpus/report.json) | The completed report now lists nine cases; the first seven include seeded review, five targeted/multisection partial failures, and long-context refusal. The later literary run failed and full-live run completed. | Original first five cases preceded accidental overlapping provider calls. Later original cases are not provider-load-controlled. Literary output has source drift; full-live starts on a different source hash from its batch manifest even though its within-case drift flag is false. Neither establishes a controlled d181 comparison. |
| [Real full-live repeat](writer-beta-evidence/d18118b-model-remaining/report.json) | Completed full Draft. | Exploratory: overlapped the still-running original corpus process. Stable source does not make this a controlled comparison. |
| [Additional real repeats](writer-beta-evidence/d18118b-model-extra/report.json) | Two listed receipts: multisection failed with a retained partial; long-context refusal. | Accidental overlapping repeat stopped; unlisted interrupted review excluded. Repeated cases are not additional distinct corpus coverage. |
| [Packaged controlled writer](writer-beta-evidence/d18118b-packaged-controlled/report.json) | Twelve named scenarios with latest runs completed. Edited text was retained in the current suggestion for edit/restart and edit/cancel/retry. | `retry_failures=true`: partial/malformed stream failures remained failed before explicit successful successor retries. `restart_between_sections` did **not** observe its boundary and is not a verified interruption case. |
| [Packaged controlled reviewer](writer-beta-evidence/d18118b-packaged-review/report.json) | Six named scenarios recorded: control, inference restart, persistence restart, cancel/retry, rate limit, transient failure. | Scripted-provider, real frozen HTTP execution; latest completion does not erase earlier failure/cancellation records. |

The newer 39 evidence above supersedes the formerly pending package/planning runs, but its planning failure then required the 1d repair. Every additional source change requires its own suite, rebuild, and isolated runtime evidence.

The [d181 separate-agent review](writer-beta-evidence/independent-d181-review.json) covered ten receipts representing eight distinct cases. It found improved standing-comment handling in the seeded reviewer (usefulness 3, compared with 1 in the earlier f2 review), with voice-sensitive advice still imperfect. It also found material defects that invalidate the candidate designation: targeted and multisection proposals cited excerpt IDs as if they were source IDs; a deleted-source proposal had wrong/nonexistent source references; both multisection receipts duplicated a Recommendation section. One uncontrolled repeat had correct citations and must not inherit another receipt's citation defect. Full-live prose restored personal stake but overgeneralized a sample exclusion. Scores remain per receipt and per dimension; no aggregate acceptance score or human review is inferred.

## Intermediate 00371f3 and f2f8f94 findings

The [003 model series](writer-beta-evidence/00371f3-model/report.json) retained an initial failed uninterrupted control (paragraph below requested length) alongside four completed intervention runs. A [repeat control](writer-beta-evidence/00371f3-control-repeat/report.json) completed, while a [targeted attempt](writer-beta-evidence/00371f3-targeted/report.json) failed on an unreadable response. Do not replace the failed control with the successful repeat without naming that selection. The [003 separate-agent assessment](writer-beta-evidence/independent-final-review.json) found substantive grounding, voice, and review defects despite reviewable recovery output.

The [f2 corpus](writer-beta-evidence/f2f8f94-model-corpus/report.json) recorded eight completed controls and an expected long-context refusal. Its [independent review](writer-beta-evidence/independent-f2-review.json), by a read-only agent who authored neither corpus nor code, documented useful targeted causal repair (evidence/citation 4; voice 3), but several negative results remained: seeded review usefulness 1 with five redundant entries across four groups; source-replacement revision/evidence/instruction scores 1; a different targeted proposal with noncanonical citations; literary review usefulness 1 and voice-sensitive advice score 1. Full-live evidence/citation scored 4 but voice 2. These are distinct observed outputs, not proof of a monotonic model improvement or of final d181 quality. The source-replacement unsupported prose and duplicate section/citation failures were retained rather than hidden behind completed statuses.

## What the earlier 86 evidence established

- Deterministic regressions exposed incomplete reviewer lenses becoming successful checkpoints, stale callbacks changing successor state, repeated unanchored comments, duplicate closing summaries, and model writes crossing student-edit/cancellation boundaries. Repairs preserve completed work and keep incomplete work visibly incomplete.
- Actual HTTP-started cancellation uncovered a separate storage defect: opening and closing a privacy-hardening descriptor discarded another SQLite connection's POSIX locks. A process regression reproduced competing writers entering the same active transaction. SQLite file preparation now preserves those locks while retaining private modes, no-follow behavior, and race checks. HTTP cancellation/retry subsequently completed successor run 2 without the earlier malformed-database error.
- Source-ledger storage retained supporting revisions, but the real reviewer still misread evidence because selected excerpts omitted useful text. A bounded saved-source reader was added so the reviewer can inspect available source text without inventing support or fetching it again. Deterministic coverage verifies availability and provenance; completed real-provider improvement is not established by this packet.
- The intermediate candidate passed **3,276 tests, with 1 skipped**. The same new regression selection failed **51 cases** against e96. Some failures represent missing new capabilities, so 51 is not a count of distinct production defects.
- The intermediate arm64 app was built and signed with the reused Apple Development identity: **78 code objects signed**, **77 objects examined by the architecture/minimum-version verifier**, bundle verification passed, and authenticated ephemeral-loopback frozen-backend smoke passed. Seven actual frozen HTTP cases were recorded: uninterrupted, three restart boundaries, cancellation/retry, partial stream, and malformed stream. The last two ended failed, preserving honest completion semantics.

The [validation receipt](writer-beta-evidence/validation-86a34f7.json) retains exact summaries, failing test node IDs, runtime hash, and original log hashes. Raw build logs and personal signing identity strings are intentionally omitted.

## Receipt inventory and limitations

Every directory retains its original report, per-case synthetic evidence, and corpus bytes where available. Reports include real durable run IDs, source/plan/comment rows, model identity, source/runtime fingerprints, and context settings. The [manifest](writer-beta-evidence/manifest.json) records original and sanitized receipt hashes. Source hashes take precedence over a Git HEAD recorded while implementation was uncommitted.

| Receipt | Evidence type and observed result | Limitation |
| --- | --- | --- |
| [e96-live-baseline](writer-beta-evidence/e96-live-baseline/report.json) | Eight real HTTP workflows using a deterministic provider; cancellation/retry produced HTTP 500 and malformed SQLite state. | Scripted inference is not a model-quality result. |
| [e96-review-baseline](writer-beta-evidence/e96-review-baseline/report.json) | Uninterrupted reviewer and cancellation/retry; cancellation failed with the same database defect. | Two scenarios only. |
| [storage-fixed-live](writer-beta-evidence/storage-fixed-live/report.json) | Ten deterministic HTTP scenarios; cancellation/retry and restart scenarios completed. Partial/malformed streams, rate limit, and transient failure remained failed. | Preliminary uncommitted source fingerprint; corpus differs from the later/e96 snapshot. |
| [storage-fixed-review](writer-beta-evidence/storage-fixed-review/report.json) | Five deterministic reviewer scenarios recorded without database corruption. | Preliminary uncommitted source fingerprint. |
| [real literary baseline](writer-beta-evidence/baseline/report.json) | Qwen3.8-27B partial reviewer output: 11 saved comments; harness timed out while run remained running. | Earliest harness omitted corpus bytes and source/effective-context fields. Although named uninterrupted, its saved warnings include restart recovery. It is not an interruption-free control. |
| [real draft baseline](writer-beta-evidence/baseline-drafts/report.json) | Three Qwen3.8-27B HTTP-started cases ended failed because empty course retrieval tried to start unavailable local embedding runtime. | No useful generated prose; long-draft budget acceptance cannot be inferred. |
| [preliminary real improvement](writer-beta-evidence/improved-live/report.json) | Missing-runtime blocker was removed; full Draft reached model inference, then reported output-token cutoff. | `code_changed_during_case=true`: exclude from controlled improvement comparisons. |
| [real model follow-up](writer-beta-evidence/final-model-live/report.json) | Stable-source full Draft reported an output-token ceiling during structured generation. | Report HEAD a119864; source hash matches the subsequent intermediate candidate. This is a failure, not completed real-model acceptance. Further schema-budget work was ongoing when this packet was assembled. |
| [86a34f7 source corpus](writer-beta-evidence/86a34f7-corpus/report.json) | Nine corpus cases using the deterministic provider, all recorded with completed run status. | Seeded plans; semantic recall, unsupported advice, revision quality, and voice remain ungraded unless separately adjudicated. |
| [86a34f7 packaged](writer-beta-evidence/86a34f7-packaged/report.json) | Seven real frozen-sidecar HTTP cases using the same deterministic provider; five completed, two failed honestly. | Not a completed real-provider quality comparison or human acceptance. |

The [86 source/package comparison](writer-beta-evidence/86a34f7-package-comparison.json) is configuration-matched and explicitly returns `acceptance: not_determined`. Its source corpus and package share corpus hash, source fingerprint, model, plan mode, and context. Source and frozen runtimes remain separately identified.

The older [deterministic defect comparison](writer-beta-evidence/deterministic-comparison.json) compares identical named case fixtures across different corpus-wide checksums. Treat it as exploratory root-cause evidence, not a configuration-matched corpus benchmark. The strict comparison utility correctly refuses differing corpus checksums.

## Separate-agent adjudication of observed real feedback

The [literary baseline adjudication](writer-beta-evidence/baseline-agent-review.json) is separate-agent review, **not human review and not blinded evaluation**. The evaluator authored the corpus and later source-reader repair, but did not produce or run this baseline output. Its grades cover the observed partial output only.

| Dimension | Observed result |
| --- | --- |
| Planning | Not applicable: saved plan; generation not evaluated. |
| Evidence/citation use | 1 on the rubric scale. Both seeds found, but omitted selected-excerpt details were incorrectly treated as unavailable evidence. |
| Revision precision | Not applicable: no replacement prose was generated. |
| Review usefulness | 1. Three false-positive evidence comments and one semantically redundant extra comment weakened prioritization. |
| Instruction following | 2. Both requested issues were found and comments preserved the document, but evidence inspection was inadequate. |
| Student voice | No rewritten-prose score. Accepted body remained byte-for-byte unchanged. |

The two seeded issues were found; missed seeds were **0 in observed partial output**. Exact duplicate comment signatures were **0**, while semantic review identified **1 redundant extra comment**. Three false-positive comments concerned details present in the full saved source. One suggested repair still attributed an unsupported declaration to the character. The source revision and selected excerpt were correctly stored; correct provenance alone did not make the feedback grounded. There were no unrelated accepted-document edits and no unsupported rewritten passages because no rewrite was produced. Unsupported feedback advice is recorded separately.

Unscored dimensions and human-review fields remain null or explicitly not-run. A deterministic provider saying a seeded draft is complete does not score any of these dimensions.

The [independent d181 recovery audit](writer-beta-evidence/d181-recovery-adjudication.json) covered 20 packaged receipts: 16 observed interventions, three controls, and one requested boundary not reached. It found accepted bodies unchanged and no exact duplicate-comment or deterministic invariant errors in its captured scope. It explicitly declines semantic/model-quality/human acceptance. The [source-attribution audit](writer-beta-evidence/source-attribution-audit.json) separately lists missing or inconsistent source fingerprints, including stable-within-case output that cannot be assigned to its batch's SHA.

## Reproduce and compare

Run from a checkout containing the harness. Use isolated synthetic profiles; do not point `--config-db` at a normal student profile. The harness creates temporary profiles and isolates credentials. These are exact **reproduction recipes**, not reconstructed historical CLI transcripts: earlier reports did not capture their full invocations. Preserve report/corpus/source/runtime hashes when repeating them.

```sh
# Frozen-source corpus, deterministic HTTP execution.
uv run python scripts/eval_writer.py \
  --source-root "$CANDIDATE_SOURCE" --fault-provider \
  --corpus docs/writer-beta-evidence/24575e9-controlled-corpus/corpus.json \
  --scenario uninterrupted --output "$OUTPUT/source-corpus"

# Existing e96 source in an independent checkout; retain all requested boundaries.
uv run python scripts/eval_writer.py \
  --source-root "$BASELINE_SOURCE" --fault-provider \
  --corpus docs/writer-beta-evidence/e96-live-baseline/corpus.json \
  --case full_live_draft_from_student_notes \
  --scenario uninterrupted --scenario restart_inference \
  --scenario restart_between_sections --scenario restart_review \
  --scenario partial_stream --scenario malformed_stream --scenario empty \
  --scenario cancel_retry --output "$OUTPUT/e96-live"

# Repeat on a completed, signed candidate bundle, using its frozen backend.
uv run python scripts/eval_writer.py \
  --source-root "$CANDIDATE_SOURCE" --backend-executable "$SIGNED_BACKEND" \
  --fault-provider --corpus docs/writer-beta-evidence/24575e9-controlled-corpus/corpus.json \
  --case full_live_draft_from_student_notes \
  --scenario uninterrupted --scenario restart_inference \
  --scenario restart_between_sections --scenario restart_review \
  --scenario cancel_retry --scenario partial_stream --scenario malformed_stream \
  --output "$OUTPUT/packaged"

# Historical86 comparison remains reproducible from its own retained receipts.
uv run python scripts/compare_writer_evals.py \
  --baseline docs/writer-beta-evidence/86a34f7-corpus \
  --candidate docs/writer-beta-evidence/86a34f7-packaged \
  --output "$OUTPUT/package-comparison.json"

# Real provider: supply only explicitly authorized isolated configuration.
uv run python scripts/eval_writer.py \
  --endpoint "$LYRA_EVAL_ENDPOINT" --model "$LYRA_EVAL_MODEL" \
  --case full_live_draft_from_student_notes --scenario uninterrupted \
  --timeout 420 --output "$OUTPUT/real-model"
```

For the frozen 24575e9 candidate, run generated planning separately from corpus-seeded planning, and keep source/package and same-package comparisons distinct. These recipes assume explicitly authorized non-loopback test configuration; omit `--allow-remote` for a loopback-only run.

```sh
# Generated planning: a distinct experiment, never pooled with saved-plan scores.
uv run python scripts/eval_writer.py \
  --source-root "$CANDIDATE_SOURCE" --endpoint "$LYRA_EVAL_ENDPOINT" \
  --model "$LYRA_EVAL_MODEL" --allow-remote --context-window 262144 \
  --generate-plan --case full_live_draft_from_student_notes \
  --scenario uninterrupted --timeout 600 --output "$OUTPUT/generated-plan"

# Same frozen package and model: one control plus explicitly observed interventions.
uv run python scripts/eval_writer.py \
  --source-root "$CANDIDATE_SOURCE" --backend-executable "$SIGNED_BACKEND" \
  --endpoint "$LYRA_EVAL_ENDPOINT" --model "$LYRA_EVAL_MODEL" --allow-remote \
  --context-window 262144 --case full_live_draft_from_student_notes \
  --scenario uninterrupted --scenario restart_inference \
  --scenario restart_between_sections --scenario restart_review \
  --scenario edit_restart --scenario edit_cancel_retry --scenario cancel_retry \
  --timeout 600 --output "$OUTPUT/packaged-model"

# Reproduce the retained final source/package and same-package comparisons.
uv run python scripts/compare_writer_evals.py \
  --baseline docs/writer-beta-evidence/24575e9-model \
  --candidate docs/writer-beta-evidence/24575e9-packaged-model \
  --output "$OUTPUT/source-package-comparison.json"
uv run python scripts/compare_writer_evals.py \
  --baseline docs/writer-beta-evidence/24575e9-packaged-model-control \
  --candidate docs/writer-beta-evidence/24575e9-packaged-model \
  --output "$OUTPUT/same-package-interruption-comparison.json"
```

The control-slice report is a documented derivation that filters the original report to uninterrupted entries; its per-case receipt and corpus bytes are unchanged. A comparison with a directory against itself would instead match identical scenarios and is not an interruption experiment. Source-only and packaged seeded controls use identical configuration but remain separately labeled. Add `--backend-executable "$SIGNED_BACKEND"` to the generated-plan recipe for the separate packaged planning gate. Explicit fault retries use `--retry-failures`; retained failed/cancelled predecessor records must still be examined.

`CANDIDATE_SOURCE` must resolve to a checkout of **24575e9f859a220440186c106a8936b303a6510e** for final reproduction. If a later evidence-only HEAD is used, disclose that HEAD and verify its backend source hash is identical to the frozen 24575e9 receipt. The harness records the source checkout's HEAD; the runtime hash independently identifies the executable actually tested. `SIGNED_BACKEND` must name the backend in the signed 24575e9 app and match the package receipt's backend hash. `BASELINE_SOURCE` must resolve to e96. `OUTPUT` is a separate evaluation-results directory. The environment variable names intentionally do not disclose a local endpoint, credential, or filesystem location.

Use the existing [local deployment procedure](local-deployment.md) for freezing, staging, bundling, stable-identity signing, bundle verification, and signed frozen-backend smoke. The executed full-suite command at 86a34f7 was `uv run --extra dev pytest -q`. The baseline failure run used the 86a34f7 test-file revision copied into the e96 source worktree, through the candidate virtual environment. Its exact test selection and the executed package-comparison arguments are retained in `validation-86a34f7.json`; only host-specific paths are replaced with named variables. Later schema-cap test additions are excluded from that 51-failure receipt. Final 24575e9 results are retained separately; intermediate receipts do not cover subsequent changes. A model comparison requires the comparison utility's explicit `--model-comparison` mode and matched non-model configuration. Do not combine a changed model and a substituted interruption control.

## Inference identity and reproducibility limits

Observed receipts retain the configured/self-reported **Qwen3.8-27B** label, tool-support probe result, and configured context of **262,144 tokens** for the ordinary real-model cases. No exact provider weight revision, model hash, or sampling seed was recorded. “Same model” therefore means the same recorded serving configuration, not independently verified weight identity or a guarantee of identical sampled prose.

The following settings are **derived from frozen 24575e9 code**, not a complete wire-level receipt of every provider request: structured writer calls request temperature 0 and an output cap of `min(generation_reserve, 4096)` (also respecting any smaller word-derived ceiling); streamed prose uses `min(generation_reserve, max(256, round(2.4 * target_words)))`. Generation reserve is `max(256, round(context_window / 4))`. Writer calls request `enable_thinking=False`, with the existing compatibility fallback when that hint is unsupported. Reviewer sampling uses its existing defaults; provider seed/default behavior was not captured. These limits preserve rejection of incomplete streams and oversized mandatory content. They are not evidence that the provider enforced every hint or supports the full configured window.

## Issue matrix for Linear / release handoff

| Issue | Implemented | Verified | Blocked / unresolved | Not run / not accepted |
| --- | --- | --- | --- | --- |
| PLA-152: grounded writer/reviewer quality | Versioned corpus/rubric; bounded saved-source access and revision provenance; mandatory-input/stream validation; source-ID and sibling-section guards; repaired live planning and internal-review source-context handoffs. | Final 24575e9 source planning/control and packaged planning/full-live/reviewer receipts; 12 separate-agent adjudications; targeted guard coverage in final controlled corpus; 3,362 tests passed/1 skipped and 11 CI checks successful. | Semantic limitations remain: voice flattening, false evidence deletions in some package samples, invented labeling/effort claims, and overstrict editorial feedback. Prior targeted real-model failures remain attributed to a37/39; they were not rerun as real targeted cases at 24575e9. | Actual human, held-out and integrated-release acceptance; different-model-family comparison; blanket beta-quality approval. |
| PLA-153: durable recovery and long sessions | Atomic run/mirror/summary publication; stale callbacks fenced; compatible edited/settled blocks retained in the current retry successor; reviewer body-version/hash checkpoints; SQLite lock repair; real HTTP fault harness and strict comparisons. | Final 33 packaged receipts: 23 controlled, 7 real full-live, 1 real planning, and 2 real reviewer; 24 observed interventions, 9 controls, and 0 selected boundaries missed. Edited text and persisted reviewer comments survive; failures stay honestly failed. Signed245 app, native isolated launch/exit, runtime hashes, full suite and CI verified. | Real edited-cancel/retry ends failed on output ceiling with partials preserved. Semantic correctness is separate from recovery. Earlier drift/overlap/normal-profile launch exclusions remain disclosed. | Long-session soak, 8 GiB device gate, integrated candidate acceptance and actual human release approval. |

No human acceptance, held-out-dataset acceptance, model-family comparison, merge, release publication, notarization, or release approval is represented by this packet. Baseline defects and intermediate negative real-model findings remain retained alongside final receipts.


## Changed files and remaining ownership

The [38-file source inventory](writer-beta-evidence/changed-files-24575e9.json) is the exact output of `git diff --name-only e96bf4886977c648f9e7905c7807c806b1ae7a80..24575e9f859a220440186c106a8936b303a6510e`. It excludes the later documentation-only evidence packet. The repairs consolidate atomic failure settlement and current-run ownership checks, and reuse existing ledger, budget, and live-block patterns. There are no new dependencies, database migrations, or competing editor redesign; this was bounded root-cause work, not a broad cleanup.

The release owner must assess the retained semantic/voice limitations and complete actual human, held-out, integrated-candidate, 8 GiB and long-session gates as applicable. This packet provides a runnable review handoff, not a waiver. Final-source/runtime/CI claims stay bound to 24575e9 even when PR #83 receives a separate evidence-only commit.

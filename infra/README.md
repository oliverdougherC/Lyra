# Lyra local infrastructure

This directory is now a historical reference, not the live desktop-runtime contract.

The current migration branch no longer treats the old bundled scrape stack as an active shipping dependency.
Any remaining infrastructure notes here are retained only to explain the older checkout-era design
and to support reading historical handoff or test evidence.

Use these docs for the live contract instead:

- `README.md`
- `docs/architecture.md`
- `docs/local-deployment.md`
- `docs/security-and-ci-gates.md`
- `docs/desktop-migration-inventory.md`

If a future desktop build needs bundled infrastructure again, document that from the checked-in
runtime and CI evidence rather than reviving the old scrape-stack notes by default.
